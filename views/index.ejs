<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weatheryze</title>
    <link rel="stylesheet" href="styles/main.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container text-center mt-5">
        <h1 class="display-4">Weatheryze</h1>
        <form action="/weather" method="POST" class="mt-4">
            <div class="mb-3">
                <input type="text" name="city" placeholder="Enter city" class="form-control " required>
            </div>
            <button type="submit">Get Weather</button>
        </form>
        <% if (locals.weather) { %>
            <h2 class="mt-4">Weather in <%= weather.name %>, <%= weather.sys.country %></h2>
            <p class="lead">Temperature: <%= weather.main.temp %>°C</p>
            <p>Weather: <%= weather.weather[0].description %></p>
            <p>Humidity: <%= weather.main.humidity %>%</p>
            <p>Wind Speed: <%= weather.wind.speed %> m/s</p>
            <% } else { %>
            <h2 class="text-danger mt-4">Please Enter a Valid City Name</h2>
        <% } %>
        <a href="/" class="btn btn-primary mt-3">Search again</a>
    </div>
</body>
</html>